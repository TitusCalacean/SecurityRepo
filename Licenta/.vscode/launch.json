{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run capture",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/capture.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "externalConsole": true,
            "MIMode": "gdb",
            "preLaunchTask": "build capture"
        }
    ]
}